import 'package:equatable/equatable.dart';import 'package:flutter/material.dart';import '/core/app_export.dart';import '../models/widget2_item_model.dart';import 'package:basri_s_application5/presentation/resume_portfolio_uploaded_screen/models/resume_portfolio_uploaded_model.dart';part 'resume_portfolio_uploaded_event.dart';part 'resume_portfolio_uploaded_state.dart';/// A bloc that manages the state of a ResumePortfolioUploaded according to the event that is dispatched to it.
class ResumePortfolioUploadedBloc extends Bloc<ResumePortfolioUploadedEvent, ResumePortfolioUploadedState> {ResumePortfolioUploadedBloc(ResumePortfolioUploadedState initialState) : super(initialState) { on<ResumePortfolioUploadedInitialEvent>(_onInitialize); on<UpdateChipViewEvent>(_updateChipView); }

_onInitialize(ResumePortfolioUploadedInitialEvent event, Emitter<ResumePortfolioUploadedState> emit, ) async  { emit(state.copyWith(resumePortfolioUploadedModelObj: state.resumePortfolioUploadedModelObj?.copyWith(widget2ItemList: fillWidget2ItemList()))); } 
_updateChipView(UpdateChipViewEvent event, Emitter<ResumePortfolioUploadedState> emit, ) { List<Widget2ItemModel> newList = List<Widget2ItemModel>.from(state.resumePortfolioUploadedModelObj!.widget2ItemList); newList[event.index] = newList[event.index].copyWith(isSelected: event.isSelected); emit(state.copyWith(resumePortfolioUploadedModelObj: state.resumePortfolioUploadedModelObj?.copyWith(widget2ItemList: newList))); } 
List<Widget2ItemModel> fillWidget2ItemList() { return List.generate(4, (index) => Widget2ItemModel()); } 
 }
